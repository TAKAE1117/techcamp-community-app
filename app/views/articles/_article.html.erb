<div class="card">
  <div class="card__body">
    <%= link_to article.title, article_path(article.id), class: :card__title%>
    <p class="card__summary">
      <%= article.text %>
    </p>
    <%= link_to "by #{article.user.nickname}", user_path(article.user), class: :card__user %>
  </div>
</div>